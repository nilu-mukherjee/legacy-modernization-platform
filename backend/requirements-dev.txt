# =============================================================================
# CodeLens AI — Development Dependencies
# =============================================================================
# Install alongside production deps:
#   pip install -r requirements.txt -r requirements-dev.txt
# =============================================================================

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# HTTP test client (also a prod dep, but listed here for clarity)
httpx>=0.27.0

# Linting & formatting
ruff>=0.5.0

# Static type checking
mypy>=1.10.0
